apiVersion: v1
kind: Service
metadata:
  name: {{ include "cena.fullname" . }}-student
  labels:
    {{- include "cena.componentLabels" (dict "root" . "component" "student") | nindent 4 }}
spec:
  type: {{ .Values.student.service.type }}
  ports:
    - port: {{ .Values.student.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cena.selectorLabels" (dict "root" . "component" "student") | nindent 4 }}
