apiVersion: v1
kind: Service
metadata:
  name: cena-ocr-sidecar
  namespace: cena-platform
  labels:
    app: cena-ocr-sidecar
spec:
  type: ClusterIP
  selector:
    app: cena-ocr-sidecar
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
      protocol: TCP
