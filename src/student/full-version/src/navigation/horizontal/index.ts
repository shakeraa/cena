// STU-W-00: minimal stub so the Vuexy horizontal layout compiles. STU-W-02
// replaces this (or this file may be deleted entirely if horizontal nav is
// not part of the student design system — the student app uses vertical
// sidebar + bottom nav, not horizontal).
import type { HorizontalNavItems } from '@layouts/types'

const navItems: HorizontalNavItems = []

export default navItems
