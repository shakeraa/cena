// STU-W-00: minimal stub so the Vuexy layout compiles. STU-W-02 replaces
// this with the real student sidebar per docs/student/01-navigation-and-ia.md.
// Do NOT copy admin sidebar items here — the student app has its own IA.
import type { VerticalNavItems } from '@layouts/types'

const navItems: VerticalNavItems = []

export default navItems
